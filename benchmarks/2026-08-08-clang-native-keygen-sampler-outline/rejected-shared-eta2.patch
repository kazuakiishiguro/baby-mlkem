diff --git a/baby-mlkem.c b/baby-mlkem.c
index 5d05579..d5ee045 100644
--- a/baby-mlkem.c
+++ b/baby-mlkem.c
@@ -879,9 +879,9 @@ static void keccakf8(__m512i st[25]) {
 }
 #else
 
-/* Encryption always has seven ETA2 streams, so specialize the sparse state. */
-static MLKEM_NOINLINE void keccakf8_sparse_eta2x7_32(
-    const uint8_t seed[32], const uint8_t nonce[8], __m512i st[16]) {
+/* Share the sparse ETA2 permutation with keygen's mixed matrix-tail lane. */
+static MLKEM_NOINLINE void keccakf8_sparse_eta2_32(
+    const uint8_t seed[32], const uint8_t *mixed_seed, __m512i *st) {
   __m512i Ba, Be, Bi, Bo, Bu, D;
   const __m512i zero = _mm512_setzero_si512();
   __m512i ba = _mm512_set1_epi64((long long)load64_le(seed + 0));
@@ -889,20 +889,55 @@ static MLKEM_NOINLINE void keccakf8_sparse_eta2x7_32(
   __m512i bi = _mm512_set1_epi64((long long)load64_le(seed + 16));
   __m512i bo = _mm512_set1_epi64((long long)load64_le(seed + 24));
   __m512i bu = _mm512_set_epi64(
-      (long long)((uint64_t)nonce[7] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[6] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[5] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[4] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[3] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[2] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[1] | (0x1FULL << 8)),
-      (long long)((uint64_t)nonce[0] | (0x1FULL << 8)));
+      0x1f00LL, 0x1f06LL, 0x1f05LL, 0x1f04LL,
+      0x1f03LL, 0x1f02LL, 0x1f01LL, 0x1f00LL);
   __m512i ga = zero, ge = zero, gi = zero, go = zero, gu = zero;
   __m512i ka = zero, ke = zero, ki = zero, ko = zero, ku = zero;
   __m512i ma = zero;
   __m512i me = _mm512_set1_epi64((long long)(0x80ULL << 56));
   __m512i mi = zero, mo = zero, mu = zero;
-  __m512i sa = zero, se = zero, si = zero, so = zero, su = zero;
+  __m512i sa = zero;
+  __m512i se = zero, si = zero, so = zero, su = zero;
+
+  if (mixed_seed != NULL) {
+    ba = _mm512_set_epi64(0, (long long)load64_le(mixed_seed + 0),
+                          (long long)load64_le(seed + 0),
+                          (long long)load64_le(seed + 0),
+                          (long long)load64_le(seed + 0),
+                          (long long)load64_le(seed + 0),
+                          (long long)load64_le(seed + 0),
+                          (long long)load64_le(seed + 0));
+    be = _mm512_set_epi64(0, (long long)load64_le(mixed_seed + 8),
+                          (long long)load64_le(seed + 8),
+                          (long long)load64_le(seed + 8),
+                          (long long)load64_le(seed + 8),
+                          (long long)load64_le(seed + 8),
+                          (long long)load64_le(seed + 8),
+                          (long long)load64_le(seed + 8));
+    bi = _mm512_set_epi64(0, (long long)load64_le(mixed_seed + 16),
+                          (long long)load64_le(seed + 16),
+                          (long long)load64_le(seed + 16),
+                          (long long)load64_le(seed + 16),
+                          (long long)load64_le(seed + 16),
+                          (long long)load64_le(seed + 16),
+                          (long long)load64_le(seed + 16));
+    bo = _mm512_set_epi64(0, (long long)load64_le(mixed_seed + 24),
+                          (long long)load64_le(seed + 24),
+                          (long long)load64_le(seed + 24),
+                          (long long)load64_le(seed + 24),
+                          (long long)load64_le(seed + 24),
+                          (long long)load64_le(seed + 24),
+                          (long long)load64_le(seed + 24));
+    bu = _mm512_set_epi64(0, 0x1f0202LL, 0x1f05LL, 0x1f04LL,
+                          0x1f03LL, 0x1f02LL, 0x1f01LL, 0x1f00LL);
+    me = _mm512_set_epi64(0, 0, (long long)(0x80ULL << 56),
+                          (long long)(0x80ULL << 56),
+                          (long long)(0x80ULL << 56),
+                          (long long)(0x80ULL << 56),
+                          (long long)(0x80ULL << 56),
+                          (long long)(0x80ULL << 56));
+    sa = _mm512_set_epi64(0, (long long)(0x80ULL << 56), 0, 0, 0, 0, 0, 0);
+  }
 
   for (int round = 0;; round += 4) {
     MLKEM_KECCAKF8_ROUND0(ba, ge, ki, mo, su, round);
@@ -925,11 +960,16 @@ static MLKEM_NOINLINE void keccakf8_sparse_eta2x7_32(
     MLKEM_KECCAKF8_ROW2(ka, ke, ki, ko, ku);
 
     if (round == 20) {
-      /* ETA2 observes words 0..15; finish only word 15 in the last row. */
-      __m512i T0 = rotl64x8(ma, 36);
-      __m512i T1 = rotl64x8(me, 10);
-      __m512i T4 = rotl64x8(mu, 27);
-      ma = MLKEM_KECCAKF8_CHI(T4, T0, T1);
+      if (mixed_seed == NULL) {
+        /* ETA2 observes words 0..15; finish only word 15 in the last row. */
+        __m512i T0 = rotl64x8(ma, 36);
+        __m512i T1 = rotl64x8(me, 10);
+        __m512i T4 = rotl64x8(mu, 27);
+        ma = MLKEM_KECCAKF8_CHI(T4, T0, T1);
+      } else {
+        MLKEM_KECCAKF8_ROW3(ma, me, mi, mo, mu);
+        MLKEM_KECCAKF8_ROW4(sa, se, si, so, su);
+      }
       break;
     }
 
@@ -941,6 +981,10 @@ static MLKEM_NOINLINE void keccakf8_sparse_eta2x7_32(
   st[4] = bu;   st[5] = ga;   st[6] = ge;   st[7] = gi;
   st[8] = go;   st[9] = gu;   st[10] = ka;  st[11] = ke;
   st[12] = ki;  st[13] = ko;  st[14] = ku;  st[15] = ma;
+  if (mixed_seed != NULL) {
+    st[16] = me;  st[17] = mi;  st[18] = mo;  st[19] = mu;
+    st[20] = sa;  st[21] = se;  st[22] = si;  st[23] = so;  st[24] = su;
+  }
 }
 #endif
 #if !defined(__clang__)
@@ -5754,7 +5798,8 @@ static void mlkem_prf_cbd_eta2x7_32(const uint8_t seed[32],
                                     poly256 out6) {
 #if defined(__clang__)
   __m512i st[16];
-  keccakf8_sparse_eta2x7_32(seed, nonce, st);
+  (void)nonce;
+  keccakf8_sparse_eta2_32(seed, NULL, st);
 #elif defined(__GNUC__)
   __m512i st[16];
   keccakf8_sparse_32(seed, nonce, NULL, st);
@@ -5774,7 +5819,8 @@ static void mlkem_prf_cbd_eta2x3x4_i8_32(
     int8_t out3[N], int8_t out4[N], int8_t out5[N], int8_t out6[N]) {
 #if defined(__clang__)
   __m512i st[16];
-  keccakf8_sparse_eta2x7_32(seed, nonce, st);
+  (void)nonce;
+  keccakf8_sparse_eta2_32(seed, NULL, st);
 #elif defined(__GNUC__)
   __m512i st[16];
   keccakf8_sparse_32(seed, nonce, NULL, st);
@@ -6735,6 +6781,8 @@ static void mlkem_keygen_prf_cbd_eta2_32_sample_tail_avx512(
   uint64_t tail_st[25];
 #if defined(__GNUC__) && !defined(__clang__)
   keccakf8_sparse_32(sigma, NULL, rho, st);
+#elif defined(__clang__)
+  keccakf8_sparse_eta2_32(sigma, rho, st);
 #else
   for (int i = 0; i < 25; i++) {
     st[i] = _mm512_setzero_si512();
